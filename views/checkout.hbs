
<div class="checkout-container">
    <div class="purchase-summary">
        <h2>Στοιχεία Αγοράς</h2>
        <div class="summary-table">
            <div class="summary-row">
                <span class="summary-label">Ημερομηνία</span>
                <span class="summary-value">{{data.date}}</span>
            </div>
            <div class="summary-row">
                <span class="summary-label">Ώρα εισόδου</span>
                <span class="summary-value">{{times.start}}-{{times.end}}</span>
            </div>
            <div class="summary-row">
                <span class="summary-label">Κανονικά εισιτήρια</span>
                <span class="summary-value">{{data.regular}}</span>
            </div>
            <div class="summary-row">
                <span class="summary-label">Παιδικά εισιτήρια</span>
                <span class="summary-value">{{data.children}}</span>
            </div>
            <div class="summary-row">
                <span class="summary-label">Φοιτητικά εισιτήρια</span>
                <span class="summary-value">{{data.student}}</span>
            </div>
            <div class="summary-row">
                <span class="summary-label">Audioguides</span>
                <span class="summary-value">{{data.audioguide}}</span>
            </div>
            <div class="summary-row summary-total">
                <span class="summary-label">Σύνολο</span>
                <span class="summary-value">{{total}} €</span>
            </div>
        </div>
    </div>
    <div>
        <h2>Στοιχεία Πληρωμής</h2>
        <form class="checkout-form" method="post" action="/buy">
            <div class="form-group">
                <label for="name">Όνομα</label>
                <input type="text" id="name" name="name" required>
            </div>
            <div class="form-group">
                <label for="surname">Επώνυμο</label>
                <input type="text" id="surname" name="surname" required>
            </div>
            <div class="form-group">
                <label for="phone">Τηλέφωνο</label>
                <input type="tel" id="phone" name="phone" required pattern="[0-9]{10}">
            </div>
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-checkbox">
                <input type="checkbox" id="amea" name="amea">
                <label for="amea">Ανάγκη για διευκολύνσεις προσβασιμότητας (ΑμεΑ)</label>
            </div>

            <input type="hidden" name="ticket" value="{{data.ticket}}">
            <input type="hidden" name="categoryId" value="{{data.categoryId}}">
            <input type="hidden" name="date" value="{{data.date}}">
            <input type="hidden" name="regular" value="{{data.regular}}">
            <input type="hidden" name="children" value="{{data.children}}">
            <input type="hidden" name="student" value="{{data.student}}">
            <input type="hidden" name="audioguide" value="{{data.audioguide}}">

            <button type="submit" class="checkout-btn">Ολοκλήρωση</button>
        </form>
    </div>
</div>
